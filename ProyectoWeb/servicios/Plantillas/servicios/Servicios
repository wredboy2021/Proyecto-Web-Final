<!DOCTYPE html>
{% extends "GestionGeneralWeb/Base" %}
{% load static %}
{% block content %}
  {% for servicios in Sers %}
  <section class="page-section clearfix">
    <div class="container">
      <div class="intro">
        <img class="intro-img img-fluid mb-3 mb-lg-0 rounded" src="{{servicios.imagen.url}}"  alt="" style="width: 110%;">
        <div class="intro-text left-0 text-center bg-faded p-5 rounded">
          <h2 class="section-heading mb-4" >
            <span class="section-heading-lower">{{ servicios.titulo }}</span>
            <span class="section-heading-upper">{{ servicios.contenido }}</span>
          </h2>
        </div>
      </div>
    </div>
  </section>
  {% endfor %}
{% endblock %}
